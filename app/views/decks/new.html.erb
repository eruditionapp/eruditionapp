<%= form_for @deck do |f| %>
  <%= render 'shared/errors', resource: @deck %>

  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :subtitle %>
  <%= f.text_field :subtitle %>

  <%= f.label :author %>
  <%= f.text_field :author %>

  <%= f.submit "Create" %>
<% end %>
