<tr class="tier-<%= deck.tier %> status-<%= deck.status %>">
  <th><%= link_to deck.title, deck_path(deck) %></th>
  <td><%= deck.subtitle %></td>
  <td><%= deck.author %></td>
  <td><%= render 'categories', resource: deck %></td>
</tr>
