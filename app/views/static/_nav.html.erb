<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Erudition', root_path, class: 'navbar-brand' %>
    </div>
    <div class="navbar-header navbar-right">
      <% if !user_signed_in? %>
        <%= link_to 'Log in', new_user_session_path, class: 'btn btn-default navbar-btn nav-pills' %>
      <% end %>
      <% if user_signed_in? %>
        <p class="navbar-text">Signed in as email@email.com</p>
        <%= link_to 'Log out', destroy_user_session_path, method: :delete, class: 'btn btn-default navbar-btn' %>
      <% end %>
    </div>
  </div>
</nav>
